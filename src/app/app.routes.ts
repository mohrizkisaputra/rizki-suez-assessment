import { Routes } from '@angular/router';
import { MonitoringDashboardComponent } from './features';

export const routes: Routes = [
    {
        path: '',
        component: MonitoringDashboardComponent,
    }
];
